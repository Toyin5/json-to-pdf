{
  "name": "json-to-pdf",
  "version": "0.1.0",
  "description": "json-to-pdf",
  "main": "src/server.ts",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "npm run build && node build/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "rimraf ./build && tsc"
  },
  "author": "Toyin5",
  "license": "AGPL-3.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.4",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
